requests>=2.31
PyYAML>=6.0
python-dateutil>=2.9
rapidfuzz>=3.6
gspread>=6.0
google-auth>=2.27
# Optional — only needed for LLM-personalized referral messages (Gemini).
# The system falls back to a smart template if this/GEMINI_API_KEY is absent.
google-generativeai>=0.8
